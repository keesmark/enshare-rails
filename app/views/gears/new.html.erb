<div class='container'>
  <div class="row">
    <div class="col-md-12">
      <%= form_for(@gear) do |f| %>
        <div class='form-sign'>
          <div class='text-center'>
            <span style="font-size: 48px; color: Dodgerblue;">
              <i class="far fa-keyboard"></i>
            </span>
            <h2 class='font-weight-normal'>
              ギア登録
            </h2>
          </div>
          <%= render 'layouts/error_messages', model: f.object %>
          <div class='form-label-group'>
            <%= f.label :title, 'タイトル' %>
            <%= f.text_field :title, class: 'form-control' %>
          </div>
          <div class='form-label-group'>
            <%= f.label :amazonUrl, 'アマゾンリンク' %>
            <%= f.text_field :amazonUrl, class: 'form-control' %>
          </div>
          <div class='form-label-group'>
            <%= f.label :image, 'ギアのイメージ', for: "exampleFormControlFile1" %>
            <%= f.file_field :image, class: 'form-control-file', id: 'exampleFormControlFile1' %>
            <%= f.hidden_field :image_cache %>
          </div>
          <div class='form-label-group'>
            <%= f.label :content, 'レビューやコメント' %>
            <%= f.text_area :content, class: 'form-control', rows: '6' %>
          </div>
        </div>
      <%= f.submit '投稿', class: 'btn btn-primary btn-block' %>
      <% end %>
    </div>
  </div>
</div>
