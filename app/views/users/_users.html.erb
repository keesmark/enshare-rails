<% if users.any? %>
  <ul class="list-group">
    <% users.each do |user| %>
      <li class="list-group-item">
        <%= user.name %>
        <p><%= link_to 'View profile', user_path(user) %></p>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>